# 服务插件设计要点

- 服务入口:业务服务类的标识(元数据中的服务名/类名)、暴露的服务方法与参数签名,
  调用入口约定(FormId + 业务对象标识)。
- 事务边界:事务从哪里开始、到哪里提交(单操作内/多步跨操作),读-判-写是否同一事务,
  事务超时与并发(锁)策略。
- 异常回滚:业务异常与系统异常的分界,回滚点、部分成功时的补偿动作,日志与提示文案。
- 调用方:服务被谁调用(单据插件/列表操作/外部接口),调用方上下文(组织/权限)如何传入,
  返回结果的契约(成功/失败码+消息)。
