# 列表插件设计要点

- 列表字段:列表要展示/管控的字段(选项来自元数据 get_form_fields),字段映射、
  列可见性、单元格只读/可编辑状态。
- 操作按钮:列表操作栏按钮(选项来自元数据 get_operations,如审核/批量操作),
  按钮触发的动作、批量选择时的逐行处理语义、按钮可用条件(选中行状态过滤)。
- 过滤条件:列表默认过滤条件、按组织/状态/时间过滤,过滤参数来源(界面输入还是调用方传入)。
- 异常处理:批量操作部分失败的行级反馈,整体回滚还是逐行成功累积,提示文案。
