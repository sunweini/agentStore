# 核心依赖
langchain>=0.3
langchain-core
langgraph>=0.2
langgraph-checkpoint-sqlite   # AsyncSqliteSaver(checkpointer,需单独安装)
langchain-openai        # DeepSeek 经 ChatOpenAI 接入(OpenAI 兼容 API)
langchain-anthropic     # 预留:后续接 Claude
langchain-chroma        # RAG 知识库向量存储(api_ref/guide/experience 三库)
langchain-huggingface   # BGE 中文嵌入(bge-small-zh-v1.5)
langchain-text-splitters
sentence-transformers   # BGE 模型推理后端
python-dotenv           # .env 加载
httpx                   # agent 侧编译服务客户端(compile_client)
python-docx             # 合同审核 agent docx 解析
pypdf                   # 合同审核 agent pdf 文本层
pymupdf                 # 合同审核 agent 扫描件 pdf 逐页渲染(OCR 云端识别)
python-multipart        # FastAPI multipart 文件上传(contract review 接口必需)

# 开发/测试
pytest

# agent1(舆情方案生成 Agent)
fastapi
uvicorn
sse-starlette>=3.4.8    # kingdee-plugin-agent api.py SSE 实时流(EventSourceResponse)
langchain-mcp-adapters
opentelemetry-sdk
opentelemetry-exporter-otlp-proto-http
opentelemetry-instrumentation-fastapi
openpyxl
pydantic
